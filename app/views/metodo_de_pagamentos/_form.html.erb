<%= form_for(@metodo_de_pagamento) do |f| %>
  <% if @metodo_de_pagamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metodo_de_pagamento.errors.count, "error") %> prohibited this metodo_de_pagamento from being saved:</h2>

      <ul>
      <% @metodo_de_pagamento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome_do_metodo %><br />
    <%= f.text_field :nome_do_metodo %>
  </div>
  <div class="field">
    <%= f.label :descricao_do_metodo %><br />
    <%= f.text_field :descricao_do_metodo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
