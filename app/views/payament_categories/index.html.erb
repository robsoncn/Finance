<h1>Listing payament_categories</h1>

<table>
  <tr>
    <th>Nome da categoria</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @payament_categories.each do |payament_category| %>
  <tr>
    <td><%= payament_category.nome_da_categoria %></td>
    <td><%= link_to 'Show', payament_category %></td>
    <td><%= link_to 'Edit', edit_payament_category_path(payament_category) %></td>
    <td><%= link_to 'Destroy', payament_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Payament category', new_payament_category_path %>
