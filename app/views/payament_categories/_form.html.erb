<%= form_for(@payament_category) do |f| %>
  <% if @payament_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payament_category.errors.count, "error") %> prohibited this payament_category from being saved:</h2>

      <ul>
      <% @payament_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome_da_categoria %><br />
    <%= f.text_field :nome_da_categoria %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
